# ClipStash Next 性能优化计划

## 目标与边界

- 用户于 2026-09-04 授权按顺序实施性能优化；每阶段实现、验证、记录后推进。
- 以当前 2.2.1 工作树为基线，保留已有未提交改动，不自动提交、推送或发布。
- 优先改善消息可操作时间、图片内存、长列表滚动及 Android 分享响应；不以开发构建耗时冒充运行时提速。
- 保持数据格式、消息顺序、复制/预览/导入语义。性能优化不得通过关闭备份或降低数据校验实现。
- 每阶段可独立回退代码；新增派生缓存必须可重建，原始图片不得覆盖。

## 基线与测量

- 已知前端生产构建：JS 278.59 kB，CSS 26.79 kB；真实应用耗时/峰值内存尚未采样。
- 固定数据集：100 / 1,000 / 10,000 条消息，分别覆盖文字、图文、多图与大图；只使用隔离合成数据，不修改用户库。
- 记录窗口冷启动、托盘唤出、首批消息可操作、首屏图片完成、分页、搜索、分享、导出耗时及峰值内存。
- 相同设备、release 构建、相同数据，区分冷缓存/热缓存；至少重复 10 次记录中位数和最慢值，较大样本再统计 P95。
- Android 用 TTID / TTFD 区分首帧与真正可用；WebView 用 Performance timeline，React 用 Profiler，SQL 用 EXPLAIN QUERY PLAN。
- 未做真机采样前，只报告自动化行为、调用次数/复杂度和构建结果，不承诺提速百分比。

## 顺序与验收门槛

| 阶段 | 状态 | 最小交付 | 验收目标 |
| --- | --- | --- | --- |
| 1. 列表与图片解耦 | 代码及自动化已验收，待实机采样 | 首屏/翻页/刷新先显示消息，后台补图；分阶段计时 | 图片请求未完成时正文和操作可用；坏图不阻塞；切页/卸载旧请求不污染；保留限并发 |
| 2. 数据库初始化与统计解耦 | 代码及自动化已验收，待实机采样 | 数据目录就绪检查只初始化一次；读图不触发 COUNT/DDL | 连续读图不重复初始化和计数；并发首次调用安全；目录切换/修复和失败重试正常 |
| 3. 图片资源与缓存 | 代码及自动化已验收，待实机采样 | 按可视区域加载；可释放资源；缩略图与原图分离；内存预算 | 折叠/屏外图片不提前读取；缓存淘汰后可重载；原图不变；预览/动画图片兼容；资源释放有测试 |
| 4. 列表渲染 | 代码及自动化已验收，待实机采样 | 隔离卡片更新；根据长列表采样实施虚拟化 | 编辑输入不重绘全部无关卡片；长列表 DOM 有界；多图展开、滚动恢复和分页不回归 |
| 5. 桌面悬浮预览 | 代码及自动化已验收，待实机采样 | 复用窗口，移除整图 localStorage 传递 | 多次预览不反复建窗口；不抢焦点；切图/关闭竞态安全；原图尺寸规则保持 |
| 6. Android 分享 | 代码及自动化已验收，待实机采样 | 图片读写后台化；用受控临时文件标识减少整图跨桥转换 | 主线程不读取/编码整图；分享队列顺序保持；错误可见；临时文件生命周期正确 |
| 7. 查询 | 代码及自动化已验收，待实机采样 | 批量读图片关联、减少计数、匹配归档索引；评估游标分页 | 合成大库执行计划和查询数量可比；同时间戳排序稳定；搜索含义不变；只按证据引入额外索引/游标 |
| 8. 导入导出 | 代码及自动化已验收，待实机采样 | 流式 ZIP；减少去重重复扫描/哈希；长任务反馈 | 峰值不随全部原图总量累加；重复包/包内重复语义保持；错误清理、校验和备份不退化 |

## 阶段外跟踪

- 首次旧库迁移目前在 setup 同步执行，含全量分页读取；阶段 2 先消除日常重复初始化，迁移后台化单独评估，必须防止用户在迁移完成前写入。
- 发现 WAL 模式下备份使用主 DB 文件复制，需专门验证可恢复性；不为性能直接取消备份。该数据安全问题不混入前端首屏修改。
- 不优先进行框架替换、全量重构、盲目拆包或增加数据库连接池。
- 数据目录搬迁目前复制整个目录，会一并复制派生缩略图；因缓存键含规范路径，搬迁后按需重建并按预算淘汰旧项，后续可让搬迁跳过专用缓存目录。

## 执行记录

- 2026-09-04：建立计划；阶段 1 开始。已有工作树改动保持原样；后续每阶段在此补充文件、命令、结果、风险和剩余实机验收。
- 后端基线：`cargo test --lib`，61 passed / 20 ignored；ignored 涉及本地数据/桌面操作，未主动运行。Cargo 实际位置为 `D:\dev-cache\.cargo\bin\cargo.exe`（PATH 可直接调用）。
- 阶段 7 预备证据：Python SQLite 内存合成 10,000 条归档消息，复现当前排序 SQL；当前索引出现 `USE TEMP B-TREE FOR ORDER BY`，10 次中位 1.548ms；候选部分表达式索引并 ANALYZE 后不再临时排序，中位 0.022ms。仅为查询形态实验，不是 Tauri 实测提速，实施前仍需用项目 bundled SQLite 和混合数据验证。

### 阶段 1 验收

- 文件：`clipstash-next/src/App.tsx`、新增 `clipstash-next/tests/App.loading.test.tsx`；旧图片交互测试改为等待图片按钮出现，适应真实异步加载。
- 首屏、翻页、刷新、导入/迁移/目录操作统一先提交消息，图片 effect 每批最多 4 张补齐；切页取消后等待旧在飞批次结束再启动新任务。
- 新增 6 项集成测试：慢图先显示正文、单图失败隔离、翻页不等待图片、切换视图不继续旧队列且并发不超过 4、翻页失败后旧页继续补图、卸载后停止后续读取。
- 主代理复查发现并修正“翻页失败令旧页一直加载中”；新增用例先失败、修正后通过。
- 验证：`npm test` 98 passed / 9 skipped；`npm run build` 通过（JS 279.54 kB、CSS 26.79 kB）。该体积小幅增加来自加载状态与计时，不代表运行变慢。
- 计时：每个 `clipstash:*` measure 仅保留最近一次，可在 WebView 开发工具执行 `performance.getEntriesByType('measure').filter(x => x.name.startsWith('clipstash:'))`。`page-data-ready` 为数据返回、`page-list-ready` 为翻页数据、`page-dom-commit` 为 React 提交、`image-preload` 为图片文件读取/转码批次；后两者均不等同屏幕真实绘制完成。
- 限制：尚未测真机耗时；旧 IPC 无法物理取消，切页后新图片可能等待当前旧批次，但文字与操作不受该等待影响。原图/300 条缓存限制本阶段保持，阶段 3 继续治理内存与按需加载。
- 回滚：只反向应用本阶段 App.tsx 与测试增量，保留原有 2.2.1 改动；没有数据库格式或原图变更。
- 下一阶段：数据库初始化与统计解耦。

### 阶段 2 验收

- 文件：`src-tauri/src/app_data.rs`、`lib.rs`、`data_transfer.rs`（均位于 `clipstash-next`）。未增加依赖和数据表。
- 成功初始化按目录缓存；命中只检查目录/DB/图片目录存在，读图不再执行 SQLite 初始化或计数。8 个并发首次调用只初始化一次，失败不缓存；手动迁移使用同一初始化锁，目录搬迁目标及显式修复使缓存失效。
- `ensure_app_data_ready()` 保持统计返回类型，统计不缓存；新增无统计 `ready_app_data_dir_path()`，用于启动、导入前检查和导入预览；已迁移库启动不再仅为丢弃结果而执行三次 COUNT。
- 新增 6 项 Rust 测试：真实独占锁下 100 次热读、8 线程初始化、失败重试、目录隔离、图片目录删除后重建、显式修复后重新初始化。
- 主代理强化证据：原 WAL 独占锁不阻止 COUNT，改为临时库 rollback journal + 独立探针明确断言 `DatabaseBusy`；同一锁保持期间图片全部读取成功且初始化次数为 1。
- 验证：主代理 `cargo test --lib` 67 passed / 20 ignored、`cargo fmt --check` 通过；加强错误码断言后专项测试再次通过；相关 `git diff --check` 通过。未运行依赖用户库/桌面操作的 ignored 测试。
- 限制：尚无实机耗时采样；文件存在检测不识别运行中外部同路径原地替换 DB，该情况需重启或显式修复。首次旧库迁移仍在 setup，后续单独处理其进度与启动路径。
- 回滚：只反向应用上述三个文件的阶段 2 增量，无数据格式变更，保留阶段 1 和原有 2.2.1 修改。
- 下一阶段：图片缩略图、可视区域加载和缓存生命周期。已核对 [Tauri 资源 URL](https://v2.tauri.app/reference/javascript/api/namespacecore/)、[image 解码限制](https://docs.rs/image/latest/image/struct.ImageReader.html)、[Blob URL 释放](https://developer.mozilla.org/en-US/docs/Web/API/URL/revokeObjectURL_static) 官方资料；需兼容数据目录搬迁以及桌面多窗口预览，不能直接把主窗口 Blob URL 当作跨窗口协议。

### 阶段 3 验收

- 文件：新增 `src-tauri/src/image_thumbnails.rs`、`src/thumbnailCache.ts`、`src/useThumbnail.tsx`；接入 `src/App.tsx`、`src/api/legacy.ts`、`src-tauri/src/lib.rs`，补充相关 `tests/`（均位于 `clipstash-next`）。
- 列表改为最长边 320px PNG 缩略图，不放大小图，保留透明度；GIF/WebP 列表使用静态首帧，预览仍读取原始文件。原图不覆盖。折叠图片不挂载；IntersectionObserver 按可视区域及 160px 预取边界触发，前端读图并发最多 4，后台生成最多 2。
- 缩略图命令在后台阻塞线程池执行，校验当前数据目录与前端预期路径。磁盘缓存位于 `.clipstash-next-thumbnails-v1`，以规范路径/大小/修改时间/版本生成键，按 128MiB 预算淘汰；首次写入扫描后维护内存索引，命中不扫描全目录。原子写入与索引登记分开，登记锁内再次核实文件状态，避免并发淘汰后计入不存在的文件。
- 限制：源文件 64MiB、40M 像素、解码器分配限制 48MiB；缓存文件最大 2MiB，尺寸和受限完整解码验证后才能命中，截断像素数据自动重建。上述是各环节约束，不是整个进程的硬内存上限。
- 前端使用 Blob URL 和共享引用计数，16MiB 压缩缩略图缓存预算；正在显示的资源保留，超预算时淘汰无引用项；离屏解除图片引用，回到可视区可复用或重载，卸载释放缓存。在飞 IPC 无法物理取消，但无消费者后不创建 URL，未开始的旧队列不再读盘。
- 原图只在预览选中后读取，串行排队并跳过已失效请求；内部预览切图/关闭释放 URL；新选文件使用可释放 Blob URL。消息和编辑器缩略图失败仍可打开存在的原图。Windows 辅助预览暂保留“选中原图 data URL + localStorage + 每次窗口”协议，阶段 5 专项替换。
- 依赖说明：复用已有 image 0.25.10，将其从 Windows 专用依赖移到通用依赖以支持 Android 缩略图，未引入新的库或数据库结构。回滚时仅还原本阶段代码和依赖位置；派生缓存可保留，不影响原图，勿覆盖其他阶段和原有 2.2.1 改动。
- 主代理最终验证：`npm test -- --run` 107 passed / 9 skipped；`npm run build` 通过，JS 282.38kB / gzip 86.30kB；`cargo test --lib` 76 passed / 20 ignored；`cargo fmt --check` 通过。Android `cargo check --lib --target aarch64-linux-android` 通过（平台未用代码等 49 条警告），不是 APK 打包或真机验收。
- 新测试覆盖：缓存去重/并发/取消/LRU/回收、折叠图片、离屏重入、原图按需读取/关闭释放、缩略图失败后的编辑器预览；Rust 覆盖原图不变/透明度/首帧/并发首次加载/缓存复用及源变化/路径范围/预算/截断及超大缓存重建。
- 阶段 1 的全页 `image-preload` 计时随批量预加载移除；`page-data-ready` / `page-list-ready` / `page-dom-commit` 保留。待真机采样首屏图片完成、滚动及峰值内存，未声称提速百分比。
- 下一阶段：隔离无关卡片重绘，并使连续加载后的列表 DOM 有界，保留多图展开、滚动恢复与分页行为。


### 阶段 4 验收

- 文件：`clipstash-next/src/App.tsx`、`src/App.css`、新增 `src/useCommittedCallback.ts` 与 `tests/App.virtualization.test.tsx`，扩展 `tests/App.loading.test.tsx`，更新 npm manifest/lock。
- 消息卡片独立 memo；稳定事件代理在 layout effect 更新最新已提交回调，输入草稿等无关更新不重新执行卡片，事件仍读取最新状态。归档期间全部按钮禁用等真实视觉变化仍更新相关卡片。
- 超过 60 条启动动态行高虚拟化，消息 ID 作为稳定键，视口上下各预留 5 条；用 ResizeObserver 测量实际行高，宽度变化时重新测量，聚焦卡片额外保留一个。消息数据和展开 ID 状态继续保留，离屏卡片 DOM/图片租约可释放。
- 修正原生 wheel 监听捕获旧分页状态；分页增加同步在飞保护，防止同一帧 wheel/scroll 重复请求；从普通列表切入虚拟化以及追加数据保留现有 offset。滚动取消桌面预览，避免虚拟行卸载不触发 mouseleave 而残留窗口。
- 编辑/归档等通用刷新保留已加载前缀长度，再恢复滚动位置；后端单次最多 100 条，因此深列表刷新按 100 条分批读取；首屏仍只读一批 30 条。数据大幅删除后只能恢复到剩余列表合法范围。多批刷新查询成本将随阶段 7 继续治理。
- 新依赖：`@tanstack/react-virtual` 3.14.10（锁定 core 3.17.8），采用成熟动态行高/测量缓存，避免自行实现滚动算法。普通安装的后续阶段长时间无输出，终止该安装会话后用 `npm install --package-lock-only --ignore-scripts --no-audit --no-fund --offline` 验证本地已获取依赖的锁文件；`npm ls` 确认依赖树有效。未声称依赖安全审计已完成。
- 参考官方 [React memo](https://react.dev/reference/react/memo)、[TanStack Virtualizer](https://tanstack.com/virtual/latest/docs/api/virtualizer)；使用稳定 item key、动态 measureElement 与焦点项 rangeExtractor。
- 验证：`npm test` 116 passed / 9 skipped；`npm run build` 通过（JS 309.32kB / gzip 94.39kB，CSS 26.96kB），`git diff --check` 通过。相对阶段 3 JS gzip 增加 8.09kB，换取有界挂载；本阶段未改 Rust，无需重跑 Rust 全套。
- 新增 9 项测试：100/1,000/10,000 合成消息在 600px 模拟视口都少于 25 张卡片，远处滚动及焦点保持，卡片跳过无关重绘且回调用最新状态，展开后的行高缓存，wheel 分页新状态，跨过虚拟化阈值/追加保留位置，归档后保持 60 条前缀及 scrollTop。测试使用受控几何尺寸，证明行为与 DOM 数量，不代表真实浏览器布局/帧率。
- 未验证：真实 WebView 拖动窗口、多图长文混合滚动、Android 惯性滚动、release Profiler/帧率；需后续真机测量。列表数据内存仍随已加载消息增长，本阶段只约束 DOM 与缩略图资源。
- 回滚：仅撤销本阶段列表、回调 hook、样式和测试，移除 TanStack 依赖/对应 lock 项，保留原有版本号和阶段 1–3；无数据格式变化。
- 下一阶段：复用桌面预览窗口，移除整图 localStorage 传递。


### 阶段 5 验收

- 文件：新增 `clipstash-next/src/desktopPreview.ts`、`reusablePreview.ts`、`imagePreview.ts` 和 Rust `src-tauri/src/image_preview.rs`，接入 App 与 command；`public/image-preview.html` 移到项目根目录作为 Vite 多入口构建页面；增加预览窗口事件 capability 及主窗口位置/大小/显示/隐藏权限。原有 image 解码库复用，未加依赖。
- 首次悬停才创建一个隐藏、不可聚焦的预览窗口；后续只更新大小、位置、标题及图片 URL，关闭时隐藏并清空图片资源。原生操作串行，ready/loaded 握手有 8 秒超时，过期请求不能让窗口再次显示；新请求取消旧加载等待；加载失败清空隐藏窗口并走既有内部预览回退。重复隐藏不重复发 IPC。
- 已有图片由后台命令校验当前数据目录及 expected_path，只返回可由 asset 协议读取的路径和元数据，逐文件动态授权，适配自定义数据目录。主窗口不再读取整图、拼接 Base64、创建探测 Image 或写入整图 localStorage。Tauri asset 协议在原生侧读取文件，不代表整个原生进程零拷贝。
- 图片尺寸考虑 EXIF 90/270 度旋转；保留原有最长边 1000px、不放大小图及屏幕边界位置算法。预览页新请求换用独立 img 元素，旧 load 回调不确认新请求；清空时移除 src。
- 尚未保存的 File 通过二进制 invoke 暂存到应用 cache 的专用文件，按 lease 释放，旧 lease 不能删除新文件；无效图片不替换当前文件。临时预览设置 128MiB 上限，超出时走内部 Blob 预览回退；已有原图不受该上传上限约束。正常关闭/切图/失败会清理；进程异常退出可能遗留一个临时文件，下次上传覆盖，未接入启动清理。
- 新增测试：窗口复用与非聚焦配置、不使用 localStorage/原图读取 IPC、旧加载完成隔离、准备期间关闭、二进制 File 与 lease 清理、加载错误后继续复用，以及预览页清空/迟到回调；Rust 用真实 EXIF JPEG 验证旋转尺寸和原图不变，验证临时文件 lease 与坏数据行为。
- 验证：`npm test` 122 passed / 9 skipped；`npm run build` 通过，多入口产物实际包含哈希 JS 引用；主 JS 308.51kB / gzip 93.98kB，共享 event JS 2.88kB / gzip 1.31kB，预览入口 0.71kB / gzip 0.39kB。`cargo test --lib` 78 passed / 20 ignored，fmt 通过；Android ARM64 cargo check 通过，保留 49 条平台未用代码等警告。
- 主代理已核对 [Tauri WebviewWindow](https://v2.tauri.app/reference/javascript/api/namespacewebviewwindow/)、[核心权限](https://v2.tauri.app/reference/acl/core-permissions/) 和本地 Tauri asset 协议源码（按魔数识别 MIME）。实际窗口不抢焦点、跨显示器/DPI、GIF/WebP 动画、冷/热预览耗时与常驻空 WebView 内存仍待 Windows 实机验收。
- 回滚：还原本阶段 App/命令/权限，恢复 HTML 原 public 路径及单入口 Vite 配置，移除新增预览模块/测试。没有原图和数据库格式变更；保留前四阶段及原有版本改动。
- 下一阶段：Android 分享的后台 IO 与临时文件标识，减少大图跨桥转换。


### 阶段 6 验收

- 文件：Android `MainActivity.kt`、新增 `ShareFileIO.kt` 及 JVM 测试；Rust 新增 `android_share.rs`、调整 `legacy_write_exec.rs` / `data_transfer.rs` / `lib.rs`；前端 App/API 与 Android 集成测试。
- 接收 Intent 时只提取文本/URI 并提交到进程内单线程 executor；ContentResolver 查询、图片读取及暂存都在后台完成，Kotlin 使用 64KiB 块复制。队列保存小型 `{shareId}` 或 `{error}`，不再保存图片 Base64；前端串行提交 ID，Rust 只接受 cache/clipstash-shares 下的受控清单和数字文件名。
- Rust 复用原有新消息写入事务及命名逻辑，按迭代器逐张读取/写入图片，一张完成后释放 bytes；不会同时把全部分享图片读入内存。后一张失败时事务回滚并清理已写图片；源 URI/原图不变。图片顺序与 PNG/JPEG 魔数命名保持。
- 当前协议新增大小约束：单图 256MiB、单次合计 4GiB（与已有数据包导入量级一致），清单 1MiB；超限或无法读取时报错，整条分享不做部分保存。上限约束文件大小，非整个进程内存上限；仍保留旧版 Base64 载荷的兼容读取，但本版原生端不再生成此载荷。
- 消费完成（含失败）清理本条受校验暂存包；异常退出遗留文件在后续启动的后台清理中按 24 小时过期，仅处理自有 UUID 目录，保留最近包与无关目录。进程内队列可跨 Activity 重建，进程被杀后不自动重放暂存包，避免在缺少持久化去重记录时重复写入；用户可重新分享。
- 连续分享失败后继续下一条，失败提示保留至关闭（最近 5 行），后续成功同时显示。新任务使用已提交的最新回调，避免 sort 更新造成消费乱序。
- ZIP 原生分享新增文件结果命令，只返回路径/计数/消息 ID，不再把全部 ZIP bytes 传入 WebView；无原生桥时保留原有 bytes/Web Share 回退。FileProvider 检查/URI 准备在后台，只有分享选择器和 Toast 在 UI 线程。导出名增加毫秒/进程/序号避免碰撞，旧导出至少保留 24 小时，避免接收应用延迟读取时被下一次导出删除；压缩写入本身留到阶段 8 流式化。
- 验证：`npm test` 125 passed / 9 skipped；`npm run build` 通过（主 JS 309.05kB / gzip 94.15kB）；Rust 80 passed / 20 ignored、fmt 通过；Android ARM64 cargo check 通过。Gradle `:app:testUniversalReleaseUnitTest` 成功，XML 确认 7 个 JVM 测试全通过（其中新增 ShareFileIO 2 项），MainActivity/Kotlin/资源实际参与 release 编译。
- 新证据：前端验证 ID 顺序、失败后继续、错误可见及原生导出不调用 bytes；Rust 验证路径限制、图片顺序、后一张失败的 DB/文件回滚、魔数命名；JVM 验证最大读取块、超限/空输入清理、过期目录清理边界。已核对 Tauri Android getCacheDir 源码返回 activity.cacheDir，与 Kotlin 暂存根一致。
- 参考 [Android 线程实践](https://developer.android.com/topic/performance/threads)、[文件访问后台化](https://developer.android.com/training/data-storage/shared/documents-files)。未验证实机跨应用 URI 权限、连续图片分享、进程被杀/重建、分享选择器及接收应用延迟读取；无 APK 发布或性能百分比。
- 无新依赖/表结构。回滚需同时撤销 Android、Rust、前端协议增量，恢复 ZIP bytes 路径与原事务函数；保留此前阶段和原有 2.2.1 修改。
- 下一阶段：消息查询批量图片关联、排序索引、计数与分页的证据化优化。

### 阶段 7 进展：批量图片关联

- 修改 `clipstash-next/src-tauri/src/legacy_query.rs` 与 `legacy_read_tests.rs`。每个非空页一次参数化 IN 查询读取本页图片（消息最多 100 条），按 message_id/id 排序后映射回消息；空页不发图片查询。原有每页 N 次图片查询降为 1 次，保留文件存在检测与详情单条查询。这里只报告 SQL 调用结构变化，未测端到端加速比。
- 修正 archived=NULL 被筛为普通消息但读取整数时报错的矛盾，分页及单条读取均映射为 false。
- 新增 205 条合成消息回归：普通/归档、升序/降序、同时间戳 ID 顺序、100 条页上限和尾页、无图/多图/孤立图片、缺失文件、空搜索结果、NULL 归档兼容。专项测试和全量 `cargo test --lib` 通过：81 passed / 20 ignored；`cargo fmt --check` 通过。前端与协议无变化，本轮未重跑前端或实机。
- 已核对 [SQLite 表达式索引](https://www.sqlite.org/expridx.html) 与 [执行计划](https://www.sqlite.org/eqp.html)：后续索引需匹配真实 ORDER BY 表达式，并用项目 SQLite 的 EXPLAIN 验证，不只凭索引名称判断收益。
- 无依赖、表结构与数据修改；回滚只撤销本次两个 Rust 文件增量，保留同文件中原有测试和其他阶段修改。
- 本阶段尚未完成：排序索引、计数成本和深页 OFFSET/游标比较仍待执行；下一步先取得实际 SQLite 执行计划，再决定索引及分页调整。

### 阶段 7 进展：排序索引

- 应用初始化新增 `idx_messages_normal_order`（COALESCE(archived,0)、created_at、id）及 `idx_messages_archive_order`（archived、COALESCE(archived_at,created_at)、id）表达式索引；只操作应用库，不为只读旧库建立索引。普通消息筛选相应使用 COALESCE，保持整数 0/NULL 都视作普通消息。
- 在项目 bundled SQLite 的隔离内存库生成 10,000 条混合记录，包含 NULL 归档、NULL 归档时间和相同时间戳；修改前后的普通/归档双向首批结果逐项相同。EXPLAIN 确认四个计划都命中新索引，StatementStatus::Sort 均从非零变成零。
- 四组 VM 指令数分别为 102243→341、78561→341、51292→341、41800→341。这是查询工作量而非耗时比例，未据此声称 Windows/Android 端到端性能。最初普通部分索引仍被 MULTI-INDEX OR + 临时排序计划取代，最终使用表达式匹配方案并重新验证通过。
- 验证：`cargo test --lib` 82 passed / 20 ignored，fmt/diff 检查通过；包含上一轮分页边界与 NULL 字段回归。未改前端。无新依赖，索引会增加少量磁盘和写入维护成本；首次升级需构建索引，真实大库初始化耗时待测。
- 回滚可撤销本次 DDL/筛选表达式/专项测试增量，现有附加索引保留不会影响旧查询；如需删除索引应在备份和确认后针对应用库执行，不能删除用户数据或覆盖其他阶段代码。
- 待完成：计数成本优化与深页 OFFSET/游标评估，然后阶段 8 导入导出。

### 阶段 7 验收：计数与分页取舍

- 分页改为先读取消息，再决定是否需要 COUNT：不足一页且非空的尾页用 offset+条数得到精确总数，首页为空直接返回 0；满页及越过末尾的空页仍查精确 COUNT。计数、消息及图片查询共用同一只读事务快照，避免在推算期间跨越并发写入版本。没有跨请求计数缓存；统计页现有计数保留，未用过期总数替换真实结果。
- 专项验证计数闭包在短页完全不调用、满页/深空页仍调用、错误继续返回；完整回归覆盖分页边界、两种视图和排序、字面量搜索、图片关联。Rust 84 passed / 20 ignored，fmt 通过。
- 项目 SQLite 隔离 10,000 条数据、重复时间戳，比较三处深度的 OFFSET 与 `(created_at,id)` 游标，返回 ID 相同。每组预编译后执行 10 次，排序样本取中间值（偶数样本取上中位）：offset=100 的 VM steps 621/1505、耗时 8.7/17.2μs；offset=1000 为 4221/3551、38.5/35.8μs；offset=9000 为 36221/1208、310.5/14.7μs。仅本机内存库热查询，不包含文件 IO、COUNT、图片状态检测、IPC 或真实设备。
- 决策：界面继续保留 OFFSET 和精确总数协议。浅页收益不成立，9000 条深度当前查询仍约 0.31ms；先处理更高成本的全库遍历。阶段 8 将对全量导入导出使用不重复 COUNT/跳过前缀的遍历方式；如真机深页成为瓶颈，再引入绑定筛选/排序上下文的界面游标，不能直接套用本测试 SQL。
- 阶段 7 完成批量关联、排序索引、可证明计数消除及游标评估；未声称真机耗时达标。回滚本轮只撤销分页快照/计数推算和对应测试，不撤销此前索引与批量关联。下一阶段为 ZIP 流式读写、导入去重成本和长任务反馈。

### 阶段 8 进展：导出图片分块

- `data_transfer.rs` 导出阶段不再将全部图片 `Vec<u8>` 收集到 staged_images；改为保存文件路径与清单元数据，使用 64KiB 固定缓冲读取、哈希、压缩。清单使用 serde_json::to_writer_pretty 直接序列化到 ZIP，去掉完整清单字节副本。
- 为保持现有含哈希的条目路径和 schema_version=1，图片先分块哈希生成路径，再分块写入 ZIP；第二遍同时核对哈希和大小，变化时报错，防止在两遍读取之间修改源图片而输出不一致清单。代价是两遍磁盘读取，换取图片内存与整包总量解耦；未声称总耗时必然下降。
- 临时 ZIP 使用 create_new，拒绝覆盖其他导出留下的同名临时文件；写入/读取/校验失败先关闭 ZIP，再清理本次临时文件。完成压缩前不替换已有目标。原有最终替换仍是删除旧目标后 rename，后续需要处理最终提交失败的保留语义；本次未弱化此行为。
- 证据：8MiB+19 的短读数据验证最大请求 64KiB、SHA256 与原内容一致、实际输出与 JPEG 魔数保持；注入中途错误验证旧目标不变、本次临时文件清除，预先存在的临时文件拒绝写入且不删除。现有导出/再导入/重复包/坏包校验测试通过；Rust 86 passed / 20 ignored，fmt 通过。
- 限制：消息和 manifest 元数据目前仍全量保留；全库读取仍分页 OFFSET；bytes 分享回退仍最终读取 ZIP 整包。导入仍一次保存一条消息的全部图片，去重仍有逐消息读图，进度反馈未实现，阶段 8 尚未完成。
- 无依赖和格式变化；回滚仅撤销 data_transfer.rs 本轮流式处理/写包回调及测试，保留阶段 6 的路径分享和临时文件保留策略。下一步先改全库遍历与导入去重，然后流式校验/落盘和进度反馈。

### 阶段 8 进展：导入去重基准

- 预览/实际导入均先收集清单中的文字+时间候选键；通过一次 messages LEFT JOIN message_images 按消息 ID/图片 ID 有序读取元数据，替代逐消息图片 SQL。只对候选消息的图片计算哈希，无关图片不打开；操作内按文件名复用哈希，采用现有 64KiB 流式读取，不再整图 fs::read。缓存仅本次操作有效，预览和导入仍各自重新验证，避免跨操作文件变化产生旧判断。
- 发现并修正相邻语义问题：旧 HashMap 在相同文字/时间时覆盖先前图片组合；改为保存全部有序图片哈希组合，避免重复包中的图片变体再次导入。缺失图片使该条既有消息无法证明完整签名，不再用空向量冒充文字消息。相较旧实现，这两类边界的去重结果会改变；包内重复保留、重复包幂等的既定目标保持。
- 新测试明确统计 hash 回调：无关文件零读取、共享图片各一次；验证多组图片组合、图片顺序，以及缺图不误匹配纯文字。既有包内重复/重复导入测试通过；Rust 87 passed / 20 ignored，fmt/diff 检查通过。未运行真机性能测试。
- 元数据仍扫描全库并在内存保留候选签名；这次消除 N+1 SQL 和无关原图 IO，不声称元数据查询与全库规模无关。无新依赖/表结构。回滚仅撤销本次去重函数与候选参数/测试，保留前面的 ZIP 流式导出。
- 下一步：全库导出遍历、导入图片分块校验/落盘、最终输出替换保护和长任务反馈；阶段 8 仍未完成。

### 阶段 8 进展：导出顺序遍历

- 移除导出的循环分页收集，改为一次只读 LEFT JOIN 查询，按 created_at、message_id、image_id 顺序逐条组装并交给导出处理。没有反复 COUNT、OFFSET 或新建数据库连接；Rust 不再保存全量 LegacyMessage 列表，只保留当前消息及必要导出清单/文件元数据。
- 选择 SQLite 连续行迭代替代手工游标分页：导出本来需要遍历全部结果，一个语句已经提供顺序读取和一致快照，无需跨批次游标协议。数据库读取语句覆盖第一遍图片哈希期间，WAL 并发写入不会改变本次消息集合；长时间读快照可能延后 WAL 回收，需实机大包测量。
- 新回归在 100/1,000/10,000 条混合消息上验证顺序、NULL 普通状态、多图顺序和缺失标记；第一条回调中由另一个连接批量归档并插入新消息，导出仍精确等于写入前集合；回调失败直接停止并返回错误。Rust 88 passed / 20 ignored，fmt/diff 检查通过。
- 图片内容仍由两次哈希校验防变化，消息快照不等于文件系统快照；清单和 ZIP 中央目录仍随条目数增长。无新依赖/数据格式；回滚仅恢复本轮导出遍历和调用方式，保留此前分块写图与去重增量。
- 下一步：导入分块校验/落盘、最终替换失败保护及长任务进度；阶段 8 未完成，实机峰值仍未采样。

### 阶段 8 进展：导入分块验证与落盘

- 移除按整条消息收集图片 bytes 的 ImportImageEntry；预览和重复消息只向 sink 分块校验，实际新增逐张写 create_new 临时文件，完成大小/SHA256 校验后按内容魔数确定扩展名并改名，随后写入图片关联。单张和单消息图片内容均不再整份常驻 Rust 内存。
- 保留 256MiB 单图、4GiB 总解压量、64MiB 清单、10 万消息限制以及 ZIP 声明大小/实际大小/哈希校验；增加读取过程中实际单图大小检查。处理重复消息也完整读完并校验，未用清单哈希绕过包内容验证。
- 临时文件创建成功即登记清理路径；任何后续读取、校验、写入、SQL 失败都会沿用整个导入事务回滚并清理本轮图片（含早先成功的图片和未完成临时图片）。包内重复与重复包逻辑保持。异常进程退出遗留临时文件的启动清理尚未增加；正常返回的错误有回滚证据。
- 新测试用两张 2MiB+19 图片验证写入块不超过 64KiB、总量边界拒绝、第二张损坏时零消息/零图片；正确包导入后，再用相同清单但损坏内容验证重复跳过路径及预览也报错，既有一条消息/两张图片保持不变。Rust 89 passed / 20 ignored、fmt 通过。
- 无新依赖/格式变化；清单、候选签名和回滚路径仍随条目数增长，bytes 导入桥仍持有压缩包，尚未宣称整个进程固定内存。回滚仅撤销本轮导入/预览流式函数和测试，不影响之前导出、查询与缓存优化。
- 下一步：最终 ZIP 替换失败保护、长任务反馈，以及完整阶段验收；阶段 8 继续进行。

### 阶段 8 进展：导出最终替换保护

- ZIP finish 后对完成的临时文件 sync_all，再关闭句柄，直接 fs::rename 替换目标；移除先删除旧导出的步骤。临时文件与目标位于同目录，正常失败清理本次临时包，旧目标不由本流程提前删除。
- 核对 [Rust fs::rename 官方说明](https://doc.rust-lang.org/stable/std/fs/fn.rename.html)，标准库支持替换已有目标并提供 Windows/Unix 实现，无需额外平台依赖。
- Windows 真实文件句柄测试以 share_mode 禁止删除/改名：完整写包后最终 rename 失败，旧目标字节保持、本次 zip.tmp 清除；关闭锁句柄后同路径重试成功，重新打开 ZIP 清单有效。全量 Rust 90 passed / 20 ignored、fmt 通过。未模拟断电、网络文件系统或底层文件系统故障，不承诺目录项断电持久性。
- 无新依赖/数据格式；回滚仅撤销本轮 finish/sync/rename 逻辑和测试。此前记录的“删除旧目标后 rename”风险在当前实现中已消除。
- 剩余：长任务反馈及阶段 8 和全目标验收；真实设备耗时和内存仍需明确标记未测。

### 阶段 8 进展：后台进度通道

- 新增 transfer_progress.rs，将进度 Channel 绑定到同步命令函数作用域；7 个导入/预览/导出 command 接收可选 JavaScriptChannelId，并在调用 Webview 上创建通道。参数缺省时继续原行为；不同命令不共享进度数据，嵌套调用和错误返回恢复原上下文。
- 接入准备、导出哈希、去重、预览校验、导入、压缩写入、最终提交阶段，以及实际分块处理字节数；已知大小阶段给出 total_bytes，未知阶段不伪造百分比。字节消息按 120ms 节流，阶段切换立即发送；发送失败不改变数据操作结果。
- 核对本地 Tauri 2.11.2 Channel 源码：Option<Channel> 不直接支持命令参数，采用可反序列化的 Option<JavaScriptChannelId> + channel_on(webview)。代码已通过命令宏编译。专项测试验证 1000 次字节累加不造成通知洪泛、定时触发值正确、嵌套隔离、错误不被替换及任务结束后无迟到进度。此前全测 90 passed / 20 ignored；新增专项 1 passed，fmt 通过。
- 前端尚未传入 Channel 或显示状态，因此用户可见的长任务反馈仍未完成。本轮只是后台基础，下轮接入前端及生命周期测试后再验收。无依赖/权限/数据格式变化，回滚撤销新增模块、命令可选参数和分块进度调用即可。

### 阶段 8 进展：界面进度与生命周期

- 新增前端 transferProgress.tsx，7 个数据处理 API 经统一包装创建独立 Channel。主界面挂载共享进度视图，显示准备/读取校验/去重/导入/写包/提交阶段及实际 MiB；有总量使用确定进度，无总量使用不定进度。没有界面订阅者的 API 调用保持原命令参数，兼容现有调用。
- 每次任务独立记录，重叠任务分别显示；成功/失败 finally 清除本次状态，迟到回调不能复活已结束任务。视图卸载解除订阅，任务本身继续；重挂载可看到仍在运行任务，后台无消费者时不触发 React 更新。状态视图单独订阅，不把每次字节更新扩散到整个 App。
- 新测试验证阶段与 1MiB/2MiB 实际计数、未知总量无伪造百分比、结束清理/旧回调隔离、一个任务失败不清除另一个，以及卸载重挂载。现有桌面设置、Android 导出/原生分享测试按可选 Channel 协议补全 mock 和断言，没有跳过用例。
- 验证：前端 128 passed / 9 skipped；npm run build 通过（主 JS 310.50kB/gzip 94.96kB，共享 event 3.70kB/gzip 1.57kB，CSS 27.37kB/gzip 5.74kB）；Rust 全测 91 passed / 20 ignored，fmt 通过。无新依赖/权限。前面“前端未接入”的状态已由本记录取代。
- 未验证实际 WebView Channel 通信与原生长任务显示流畅度；接下来做 Android 编译和全阶段证据审查，明确保留真实设备性能/资源采样缺口。回滚需同时撤销前端包装/视图/样式/测试和后台进度协议，保留已验证的数据流优化。

## 综合验收审查（2026-09-04，首次审查记录；最终结果见文末）

| 要求 | 当前实现与证据 | 判定 |
| --- | --- | --- |
| 首屏正文先于图片 | App commitPage 直接显示；App.loading 测试覆盖未完成图片、失败、切页与卸载 | 自动化通过，真实首屏耗时未测 |
| 就绪与统计解耦 | app_data 成功路径缓存；并发首调/失败重试/目录失效/数据库锁测试 | 自动化通过 |
| 可视缩略图与资源回收 | useThumbnail/thumbnailCache/image_thumbnails；折叠/离屏/缓存/预算/原图不变测试 | 自动化通过，峰值内存未测 |
| 长列表与卡片更新 | MessageList/useVirtualizer/memo；100/1000/10000 模拟几何、焦点/行高/翻页测试 | 自动化通过，真实帧率未测 |
| 桌面预览复用 | reusablePreview/desktopPreview/imagePreview；串行复用、非聚焦配置、迟到回调、EXIF/lease 测试 | 自动化通过，实际多屏/焦点/动画未测 |
| Android 分享后台化 | MainActivity executor、ShareFileIO、android_share；原生文件路径、ID 顺序/失败、Rust 回滚与 JVM 测试 | 编译/自动化通过，跨应用真机未测 |
| 查询成本 | 每页批量图片关联、两种排序索引、短页免 COUNT、同快照；实际 bundled SQLite 计划/VM steps、边界/搜索测试 | 自动化通过；界面 OFFSET 按测量保留 |
| ZIP/去重/反馈 | 图片分块、连续关联遍历、候选文件哈希、图片变体签名、最终替换、独立 Channel；损坏回滚/重复校验/实际文件锁/前端生命周期测试 | 后台及反馈自动化通过，文件选择桥仍需优化 |

- 最新 Android ARM64 cargo check 成功（50 条平台/未用代码警告）；Gradle :app:testUniversalReleaseUnitTest 成功，本轮为 up-to-date，XML 共 7 项测试、零失败/错误。前端最近全测 128 passed / 9 skipped，Rust 最近全测 91 passed / 20 ignored，构建与 fmt/diff 通过。
- adb devices -l 实际返回空设备列表。未新建或安装 APK，未进行 Windows/Android release 实机计时、峰值内存、跨应用权限或预览焦点验收；计划中的 10 次设备耗时采样没有伪报完成。
- 发现全链路缺口：App.importDataPackageFile 仍先 fileToNumberArray，整份 ZIP 在 JS 数字数组及 IPC 中膨胀。下一步使用串行分块二进制上传到受控临时 ZIP，按声明大小/偏移验证，失败清理后复用现有导入。该项未落地前不将阶段 8 或总体目标标为完成。
- TASK_STATE.md 合并了中间阶段的互相矛盾状态；具体历史与验证结果保留在本计划。未提交、推送或发布；原有无关改动保留。

### 阶段 8 补齐：文件选择的分块二进制传输

- 新增 zip_upload.rs / zipFileUpload.ts，文件选择入口不再 fileToNumberArray。按 1MiB Blob.slice 读取并使用二进制 invoke 串行发送，后台确认下一偏移后才读下一块；不再创建整包 JS 数字数组。传输与后续导入共用进度任务，传输阶段显示实际已确认字节数。
- 后台 begin 只接受 ZIP 名称和非零声明大小，生成应用 cache/zip-uploads 内的唯一暂存标识；append 从请求头取标识/偏移，只接受非空且不超过 1MiB 的二进制块，检查顺序及累计大小；finish 必须精确达到声明大小，关闭写入句柄后复用现有流式导入。没有新增整包大小限制，沿用 ZIP 解压/清单限制。
- 每个上传独立句柄与路径；普通错误、显式 abort、finish 成功/失败通过作用域清理关闭并删除暂存文件，前端读块/发送/确认/导入失败也发送幂等 abort。输入不能指定任意写入路径，临时文件 create_new，暂存根拒绝符号链接。旧 bytes command 保留兼容，但当前文件选择流程不再调用。
- 新验证：Rust 测块上限、错位、超声明、未传完整拒绝、准确内容以及成功/失败析构清理；前端验证 2MiB+7 文件只分三块读取、等待确认期间不读下一块、二进制类型及偏移，以及读取/写入/确认/finish 四类错误都会 abort。Android 界面测试实际走 begin/append/finish 并断言旧 bytes command 未调用。
- 最新前端 133 passed / 9 skipped，生产构建通过（主 JS 311.25kB/gzip 95.26kB）；Rust 92 passed / 20 ignored、fmt 通过。无新依赖/数据格式/整包上限；需复查 Android 最新构建与异常终止暂存生命周期后收尾。
- 限制：进程崩溃或 Webview 完全销毁时不能依赖前端 catch，当前暂存登记只在内存，旧进程残留文件的过期回收仍需补齐；真实 WebView 二进制请求头通信未实测。回滚只撤销新增上传模块、四个 command、文件选择调用和进度包装增量，不覆盖其他导入导出优化。

### 阶段 8 最后补齐：过期上传回收

- 每次开始新上传时回收闲置超过 24 小时的上传登记，以及目录内超过 24 小时、名称严格匹配 upload-数字-数字-数字.zip 的普通残留文件；不跟随符号链接、不删除近期或无关文件。不是后台定时扫描，没有新上传时不会主动清扫。
- 活跃路径独立于上传登记保留到 Upload 析构：finish 移出登记并开始导入后，仍保持保护，因此长导入不会被下一次清理误删。每次成功 append 刷新活动时间；正常结束、错误与 abort 关闭句柄并清理，失败删除的残留可由后续过期清理重试。
- 新测试验证过期孤儿与闲置登记回收、已完成上传但仍被导入持有的文件保留、近期/无关/不合命名文件保留。测试使用可控时钟推进，避免 Windows 开机不足 24 小时导致 Instant 向前减时溢出。

## 最终验收结果

- 八阶段的实现和可执行自动化验证已完成；首次综合审查发现的 ZIP 文件选择整包传输与暂存生命周期缺口均已补齐。上面的中间状态按执行顺序保留，不代表仍需实现的事项。
- 前端最新全测：133 passed / 9 skipped；生产构建通过，主 JS 311.25kB/gzip 95.26kB。Rust 最新全测：93 passed / 20 ignored，cargo fmt --check 通过。最新 Android ARM64 cargo check 通过（50 条未用代码/平台等警告）；release Kotlin/JVM 任务成功，XML 7 tests、0 failures、0 errors，最近检查为 up-to-date。git diff --check 通过，仅工作树 LF/CRLF 提示。
- 实现证据闭合：首屏数据先展示；就绪缓存不重复统计；按可视区域获取缩略图并释放资源；卡片 memo/虚拟列表；桌面预览窗口复用；Android 分享后台暂存/ID 协议；批量关联、匹配排序索引、精确计数优化及分页评估；ZIP 图片/文件选择分块、顺序遍历、候选去重、错误清理、最终替换及实际进度。对应测试与实现位置见阶段记录和综合审查表。
- 未验证：真实 Windows/Android release 冷热启动、10 次设备耗时采样、滚动帧率、进程峰值内存、预览焦点/多屏/DPI/动画、实际跨应用权限及 WebView 二进制/Channel 通信。adb 已确认无连接设备；这些缺口按目标要求明确记录，未声称性能百分比、真实设备达标或 APK 发布完成。
- 当前已知取舍：缩略图/GIF 首帧与各环节大小限制；虚拟化新增约 8kB gzip 依赖成本；窗口复用保留一个空 WebView；导出两遍图片读取及较长 WAL 读快照；清单/索引元数据仍随条目数增长；无原生桥的导出分享保留 bytes 回退。数据库 WAL 备份、首次旧库迁移后台化等阶段外事项继续单列，不以取消备份换速度。
- 所有变更留在当前工作树，未提交、推送、发布或更改用户真实数据。回滚按各阶段记录只反向应用本任务增量；保留原有 2.2.1 改动，不整文件覆盖含其他修改的文件。
- 下一步建议是隔离 release 测试数据做 Windows/Android 实机验收；这是后续测量与发布决策，不把当前自动化验收表述为实机验收。
