# ClipStash Next 功能优先 TODO

> 目标：先把 Tauri 版推进到“日常可用、可安全替代旧版一部分工作流”，模块整理暂时降级为只在功能需要时顺手做。

## 当前判断

- 当前整体进度按“可替代旧 Python 版日常使用”估算约 `85%`。
- 阶段 0/1 已基本完成。
- 阶段 2/3 的核心读写、复制、导入能力已经具备，并做过大量真实 UI 验收。
- 主要短板不在底层读写，而在真实系统环境验收：托盘、全局快捷键、开机自启动、安装包迁移和用户侧发布前走查。
- 暂停继续做纯模块拆分；除非为功能落地必要，不再优先整理 `legacy_data.rs` 这类内部细节。

## P0：先做成可安心使用

### 1. 数据安全面板

- [x] 在 UI 中显示当前旧库路径、图片目录、DB 是否存在、图片目录是否存在。
- [x] 显示当前普通/归档/总数、图片关联数、孤立图片数。
- [x] 显示最近 DB 备份列表，至少包含文件名、时间、大小。
- [x] 显示最近图片备份目录列表。
- [x] 提供“打开数据目录”“打开备份目录”按钮。
- [x] 当只读审计发现基线变化时，在 UI 中提示“旧库可能被其他版本改动过”。
- [x] 验收：不写 DB；`npm run verify:legacy-readonly` 通过；真实 UI 能看到当前路径和备份。

### 2. 日常主流程打磨

- [x] 统一新增/编辑/删除/归档/恢复/复制/导入后的成功提示样式。
- [x] 统一失败提示，失败时保留弹窗状态，不丢输入。
- [x] 新增/编辑弹窗里明确显示本次操作会写旧库，并展示备份策略。
- [x] 删除弹窗突出显示“会备份 DB 和图片后删除”，避免误删真实历史数据。
- [x] 列表刷新后保持当前视图、排序和滚动位置尽量不跳。
- [x] 验收：创建临时消息、编辑、归档、恢复、删除全流程可通过 UI 完成并清理。

### 3. 图片与列表体验

- [x] 多图消息的展开/收起交互可用，不出现布局跳动或按钮挤压。
- [x] 图片缺失时显示明确占位和文件名，不出现空白块。
- [x] 图片预览支持上一张/下一张。
- [x] 已归档列表大量消息时保持可滚动、不卡死。
- [ ] 待用户验收：使用包含多图、缺图、已归档大列表的旧库做 UI 检查。

## P1：替代旧版的系统能力

### 4. 设置页

- [x] 读取旧 `settings.json`，缺失字段提供默认值。
- [x] 保存设置时由 Rust 写入，并返回实际生效值。
- [x] 设置项至少覆盖：窗口置顶、导入后归档、粘贴间隔、快捷键、开机启动。
- [x] 保存失败时显示明确错误，不伪装成成功。
- [ ] 待用户验收：保存后重启 Next，设置仍生效；旧 Python 不因新增字段崩溃。

### 5. 托盘与窗口行为

- [x] 实现托盘驻留。
- [x] 托盘菜单包含：显示/隐藏主窗口、打开数据目录、退出。
- [x] 关闭窗口时按设置决定隐藏到托盘或退出。
- [x] 与旧版对齐：主窗口关闭默认隐藏到托盘，托盘退出才结束进程。
- [ ] 待用户验收：托盘菜单可操作，退出后进程结束。

### 6. 全局快捷键

- [x] 支持设置并注册全局呼出快捷键。
- [x] 支持设置并注册全局“导入当前剪贴板/快速保存”快捷键。
- [x] 快捷键冲突时显示错误。
- [x] 重启后快捷键仍生效。
- [ ] 待用户验收：从其他窗口按快捷键能呼出 ClipStash Next。

### 7. 剪贴板监听和快速保存

- [x] 明确是否需要替代旧版自动保存剪贴板能力：本阶段不做后台监听，保留全局快速保存和主窗口粘贴入口，避免无感写入。
- [x] 如需要，实现文字/图片剪贴板读取预览。
- [x] 支持从剪贴板快速保存文字、图片、图文。
- [x] 支持主窗口 `Ctrl+V` / `Shift+Insert` 读取当前剪贴板并打开统一新建/编辑窗口。
- [ ] 待用户验收：复制文字/图片后可通过 Next 保存到新库，重复触发不写旧库。

### 8. 开机自启动

- [x] 设置页开机自启动不再只保存偏好，需要实际接入 Windows 启动项或 Tauri 自启动插件。
- [x] 设置失败时显示明确错误，并回滚 UI 状态到实际值。
- [ ] 待用户验收：重启系统或注销登录后验证自启动行为。
- [ ] 待用户验收：开关状态重启应用仍准确，系统启动项和 UI 一致。

### 9. 设置持久化

- [x] 设置由 Rust 读写到新应用配置文件，启动时返回实际生效值。
- [x] 从 `localStorage` 迁移已有设置，迁移后以前端展示 Rust 配置为准。
- [x] 设置项覆盖：窗口置顶、导入后归档、粘贴间隔、快捷键、开机启动、悬浮预览延迟、滚动速度、字体大小、排序。
- [ ] 待用户验收：保存后重启 Next，设置仍生效且不会显示虚假成功。

## P2：发布替换准备

### 10. 更新检查

- [x] 实现 GitHub Release 检查。
- [x] 比较当前版本与 GitHub latest release 版本。
- [x] 展示 release notes 和可下载 Windows 安装包入口。
- [x] 检查失败时显示失败原因。
- [x] 检查失败或有更新时提供可点击 Release 页面入口。
- [x] 验收：断网/HTTP 失败/响应缺字段场景不会只有“待实现”或模糊提示。

### 11. 安装包和回滚

- [x] 固化 `npm run tauri build` 的发布产物路径和命名。
- [x] 安装包默认使用新数据目录 `%APPDATA%\ClipStash Next`，首次启动只读迁移旧数据。
- [x] 首次启动迁移旧库时跳过重复数据，不移动、不删除旧 `clipstash.db` / `images/`。
- [x] 明确旧 Python 版回滚方法。
- [ ] 待用户验收：安装后可迁移旧库；旧 Python 仍能打开原旧库。

### 12. 真实验收数据集

- [x] 准备或生成回归测试 DB，覆盖纯文字、单图、2-4 图、18 图、已归档、纯图片、长文本、缺失图片。
- [x] 避免反复直接用真实用户库做破坏性验收。
- [x] 将回归数据集路径和使用方式写入文档。
- [x] 验收：CI 或本地脚本可对测试 DB 跑只读一致性检查。

## 暂缓事项

- [ ] 暂缓继续拆 `legacy_data.rs` 内部测试和模块，除非阻碍功能开发。
- [ ] 暂缓影子索引 `message_index`，等 P0/P1 可用闭环完成后再做。
- [ ] 暂缓新 schema 主库切换，继续以旧 schema 兼容模式为默认。

## 原计划完成度对照

| 原计划阶段 | 当前状态 | 说明 |
| --- | --- | --- |
| 阶段 0：Tauri 模板、旧目录定位、只读计数 | 基本完成 | `clipstash-next` 已创建，能定位旧库并读取计数。 |
| 阶段 1：只读消息列表、缩略图、排序 | 基本完成 | 普通/归档、排序、分页、图片存在状态、预览已实现并验证。 |
| 阶段 2：基础写入 | 主要完成 | 新增文字/图片/图文、编辑文字、替换图片、删除已实现；真实 UI 验收覆盖主路径。 |
| 阶段 3：归档、恢复、复制、导入 | 主要完成 | 归档/恢复、复制文字/图片、导入队列、目标窗口、单项/整队列粘贴、粘贴后归档均已实现并验收。 |
| 阶段 4：系统能力 | 未完成 | 托盘、全局快捷键、开机启动、主页剪贴板快捷导入、设置持久化、更新检查仍是主要缺口。 |
| 阶段 5：发布替换 | 部分完成 | 曾构建过安装包，但还没有形成稳定发布、首次启动审计、回滚流程。 |

## 下一步推荐顺序

1. 先做 P0-1：数据安全面板。
2. P0-2：日常主流程提示和弹窗打磨。
3. 当前进入 P0-3：图片与列表体验。
4. 再做 P1-4：设置页。
5. 然后做托盘、快捷键、更新检查。
